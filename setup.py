from setuptools import setup, find_packages

setup(
    name='endpoints',
    version="0.1",
    description="endpoints: Server-side endpoints for 0x2048",
    include_package_data=True,
    packages=find_packages(),
)
