import React from 'react';
import { Text } from './styles';
import * as copy from '../../../copy';

const Waiting = (): JSX.Element => {
    return <Text>{copy.WAITING}</Text>;
};

export default Waiting;
